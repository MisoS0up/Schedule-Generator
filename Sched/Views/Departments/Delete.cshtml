@model Sched.Models.Domain.Department

@{
    ViewData["Title"] = "Delete";
}

<div class="container">
    <div class="text-center">
        <h1>Delete Department</h1>

       
        <div>
          
            <hr />

            <div class="text-center">
                <p><strong> @Html.DisplayNameFor(model => model.Name)</strong> : @Html.DisplayFor(model => model.Name) </p>
            </div>


            <h3>Are you sure you want to delete this?</h3>
            <br />

            <form asp-action="Delete">
                <input type="hidden" asp-for="DepartmentId" />
                <button type="submit" value="Delete" class="btn btn-outline-danger" > <i class="fas fa-trash"></i>Delete</button> |

                
                <a class="btn btn-warning"asp-action="Index"> <i class="fas fa-list"></i> Back to List </a>
            </form>
        </div>
    </div>
</div>

