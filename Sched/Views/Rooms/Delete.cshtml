@model Sched.Models.Domain.Room

@{
    ViewData["Title"] = "Delete";
}


<div class="container"> 
    <div class="text-center">
        <h1>Delete Room</h1>

    
        <div class="text-center">
  
            <hr />

            <p><strong> @Html.DisplayNameFor(model => model.Name)</strong> : @Html.DisplayFor(model => model.Name) </p>

   <br />

            <h5 class="m-4">Are you sure you want to delete this?</h5>
            <form asp-action="Delete">
                <input type="hidden" asp-for="RoomId" />
                <button type="submit" value="Delete" class="btn btn-danger" class="btn btn-outline-danger"><i class="fas fa-trash"></i> Delete</button> |
                <a  class="btn btn-warning"asp-action="Index"><i class="fas fa-list"></i> Back to List</a>
            </form>
        </div>

</div></div>
